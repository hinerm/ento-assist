{
	"servers": {
		"ento-assist": {
			"type": "stdio",
			"command": "uv",
			"args": [
				"--directory",
				"${workspaceFolder}",
				"run",
				"ento-assist"
			],
			"env": {
				"ENTO_DB_PATH": "${userHome}/.entoassist/db.sqlite"
			}
		}
	}
}
